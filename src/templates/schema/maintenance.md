# Maintenance

Keep the vault healthy:

- Fix broken wikilinks.
- Add missing frontmatter.
- Connect orphan pages when a meaningful relationship exists.
- Rebuild `.notewell/` after structural changes.
